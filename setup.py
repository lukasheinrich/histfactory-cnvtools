from setuptools import setup,find_packages

setup(
  name = 'histfactory-cnvtools',
  version = '0.0.1',
  packages = find_packages(),
  author = 'Lukas Heinrich',
  author_email = 'lukas.heinrich@cern.ch',
  description = 'simple tools to extract informaiton from histfactory configurations (e.g. for HepData)',
)